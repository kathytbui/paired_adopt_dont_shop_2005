<% if @favorites.empty? == true %>
  You have not favorited any pets!
<% else %>
  <% @favorites.each do |favorite| %>
    <%= link_to "#{favorite.pet.name}", "/pets/#{favorite.pet_id}" %>
    <%= favorite.pet.image %>
    <%= link_to "Remove Me From Your Favorites", "/favorites/#{favorite.pet_id}", method: :delete %>
  <% end %>
<% end %>

<%= link_to "Remove ALL Favorites", "/favorites", method: :delete %>


<%= link_to "Adopt A Pet", "/applicant_pets/new" %>
